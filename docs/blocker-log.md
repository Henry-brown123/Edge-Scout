# Blocker log (rule 22)

One entry per blocker found during research. Each says whether it is
permanent or only current, what would unblock it, and when to revisit.

| Date | Blocker | Permanent or current? | What would unblock it | Worth it now? | Revisit when |
|---|---|---|---|---|---|
| 2026-09-22 | Corners / cards / shots **markets** have no sharp price through the Odds API for League One/Two (keys invalid or priced by nobody) | Current — a provider limitation, not a market one; Pinnacle and Betfair price corners and cards | A second odds provider: Pinnacle's own API (B2B agreement) or Betfair Historical Data Service (paid, exchange prices; EFL corners coverage to be confirmed) | No — the totals result shows Pinnacle's information advantage in goals markets; corners/cards would face the same information problem plus a new provider cost | If a provider is added for any other reason, or if a lineup-aware model shows skill in totals |
| 2026-09-22 | Corners / cards **outcomes** not in `fixture-stats.json` (only xG, shots on, total shots, possession are stored) | Current — API-Sports statistics carry corner kicks, yellow and red cards; our fetch drops them | Extend `fetch` in the stats backfill to store corners/cards (no extra calls where stats are already fetched; re-fetch for older fixtures) | Only alongside a market source | With the market entry above |
| 2026-09-22 | Historical **referee** not held (API-Sports fixture objects include `fixture.referee`; the pool trims it) | Current — trivially re-fetchable | One `/fixtures?league&season` call per league-season (~30 calls for L1/L2 all seasons) stored as a fixture field | Yes, cheap | Before any model that wants referee tendencies (cards, totals) |
| 2026-09-22 | Historical **weather** not held (live uses Open-Meteo forecast only) | Current — Open-Meteo's archive API (ERA5, free) gives hourly weather by lat/lon back to 1940; venue coordinates already exist | One archive call per venue-day (batchable by venue and date range) | Cheap; value unproven — worth it as part of a lineup-aware totals retry, not alone | With a lineup-aware retry |
| 2026-09-22 | **Pre-match confirmed lineups** not persisted: the lineup-triggered lock records only whether team sheets were present (`lineupsAtLock`, `lineupSeenAt`), not the XI | Current — the poll already fetches the sheets; persistence is ~10 lines. Retroactively, the post-match lineups pool (`lineups.json`, 15,115 EFL fixtures from 2022) holds the actual starting XI, which equals the confirmed pre-match XI in all but rare warm-up changes | Persist the polled sheets on the watching/bet record at lock; for history, use the post-match XI as the pre-match proxy | Yes — the polling cost is already paid | Now (recommended in Addendum 69 Part 4) |
| 2026-09-22 | BTTS / team totals: Pinnacle history through the Odds API only from ~2024, soft-book-led | Current — depth grows a season per season | Nothing to build; wait, or pull `btts` per event from the historical endpoint when two Pinnacle seasons exist | Not now | Autumn 2027, or with a lineup-aware model |
